@model IEnumerable<DetailsQuizViewModel>

@{
    ViewData["Title"] = "Deleted Quizzes";
    Layout = "_Layout";
}


<link rel="stylesheet" href="~/css/Admin/Quiz/deleted-quizzes.css" asp-append-version="true" />
<script src="~/js/Admin/Quiz/deleted-quizzes.js" asp-append-version="true"></script>

<!-- Page header -->
<div class="page-header">
    <div class="page-header-left">
        <a asp-area="" asp-controller="Quiz" asp-action="Index" class="back-btn">
            <svg width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <polyline points="15 18 9 12 15 6"/>
            </svg>
            Back
        </a>
        <div>
            <h1>Deleted Quizzes</h1>
            <p class="subtitle">Review, restore, or permanently remove deleted quizzes.</p>
        </div>
    </div>
</div>

<partial name="_Alerts" />

<!-- Stats -->
<partial name="_QuizStats" model="@Model"/> 
<!-- Toolbar -->
<div class="toolbar">
    <div class="toolbar-left">
        <div class="search-wrap">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>
            </svg>
            <input id="searchInput"
                   class="search-input"
                   type="search"
                   placeholder="Search by title or ID…"
                   oninput="filterTable()" />
        </div>
    </div>
    <button class="btn btn-outline" onclick="window.location.reload()">
        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/>
        </svg>
        Refresh
    </button>
</div>

<!-- Table -->
<div class="table-wrap">
    @if (!Model.Where(q=>q.IsDeleted == true).Any())
    {
        <partial name="_EmptyState" model='@("No deleted quizzes found.")' />
    }
    else
    {
        <table id="deletedTable">
            <thead>
                <tr>
                    <th>Quiz</th>
                    <th>Description</th>
                    <th>Start time</th>
                    <th>Questions</th>
                    <th>Status</th>
                    <th style="width:160px;">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var quiz in Model.Where(q=>q.IsDeleted == true))
                {
                    <partial name="_QuizDetails" model="@quiz"/>
                }
            </tbody>
        </table>

        <div class="pagination-bar">
            <span id="resultCount">Showing @Model.Count() deleted quiz(zes)</span>
        </div>
    }
</div>


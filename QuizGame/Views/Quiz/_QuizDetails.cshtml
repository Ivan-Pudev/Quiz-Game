@model DetailsQuizViewModel
<tr data-title="@Model.Title.ToLower()" data-id="@Model.Id.ToString().ToLower()">

    <td>
        <div class="quiz-cell">
            <div class="quiz-icon">
                <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
                    <rect x="3" y="3" width="18" height="18" rx="2" />
                    <path d="M7 8h10M7 12h10M7 16h5" />
                </svg>
            </div>
            <div>
                <div class="quiz-title">@Model.Title</div>
                <div class="quiz-id">@Model.Id</div>
            </div>
        </div>
    </td>

    <td>
        <div class="desc-text" title="@Model.Description">@Model.Description</div>
    </td>

    <td>
        <div class="date-text">@Model.StartTime.ToString("dd MMM yyyy")</div>
        <div class="date-sub">@Model.StartTime.ToString("HH:mm") UTC</div>
    </td>

    <td>
        <span class="count-badge">
            <svg width="11" height="11" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="10" />
                <path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3" />
                <line x1="12" y1="17" x2="12.01" y2="17" />
            </svg>
            @Model.Questions.Count()
        </span>
    </td>

    <td>
        <span class="badge-deleted">
            <svg width="10" height="10" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
                <polyline points="3 6 5 6 21 6" />
                <path d="M19 6l-1 14H6L5 6" />
            </svg>
            Deleted
        </span>
    </td>

    <td>
        <div class="actions-cell">
            <form asp-area=""
                  asp-controller="Quiz"
                  asp-action="RestoreQuiz"
                  asp-route-id="@Model.Id"
                  method="post"
                  onsubmit="return confirm('Restore quiz \'@Html.Raw(Model.Title)\'?');">
                @Html.AntiForgeryToken()
                <input type="hidden" name="id" value="@Model.Id" />
                <button type="submit" class="btn btn-restore btn-sm">
                    <svg width="12" height="12" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
                        <polyline points="23 4 23 10 17 10" />
                        <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10" />
                    </svg>
                    Restore
                </button>
            </form>

            <form asp-area="Admin"
                  asp-controller="Quiz"
                  asp-action="DeleteQuiz"
                  asp-route-id="@Model.Id"
                  method="post"
                  onsubmit="return confirm('Permanently delete \'@Html.Raw(Model.Title)\'? This cannot be undone.');">
                @Html.AntiForgeryToken()
                <input type="hidden" name="id" value="@Model.Id" />
                <button type="submit" class="btn btn-delete-ghost btn-sm">
                    <svg width="12" height="12" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                        <polyline points="3 6 5 6 21 6" />
                        <path d="M19 6l-1 14H6L5 6" />
                        <path d="M10 11v6M14 11v6" />
                        <path d="M9 6V4h6v2" />
                    </svg>
                    Delete
                </button>
            </form>
        </div>
    </td>

</tr>